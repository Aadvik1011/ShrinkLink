This is the folder for the Views part of the MVC Model

this provides us with the server side rendering 
with the use of different tools such as EJS

This is done by : -
1 : import the path module(built-in)
2 : set the views of the ejs respectively